<Window x:Class="EditorDbf.App.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Entrada de datos" 
        SizeToContent="WidthAndHeight" 
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource AppBackgroundBrush}"
        Foreground="{DynamicResource PrimaryTextBrush}"
        Width="400"
        MinWidth="350">
    <Border Padding="24" Background="{DynamicResource LayerBackgroundBrush}" BorderBrush="{DynamicResource PanelBorderBrush}" BorderThickness="1" CornerRadius="8" Margin="12">
        <StackPanel>
            <TextBlock x:Name="PromptLabel" 
                       Text="Ingrese el valor:" 
                       FontSize="14" 
                       FontWeight="SemiBold"
                       Margin="0,0,0,12"
                       TextWrapping="Wrap"/>
            
            <TextBox x:Name="InputTextBox" 
                     FontSize="14" 
                     Padding="8,6"
                     Margin="0,0,0,24"
                     VerticalContentAlignment="Center">
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=AcceptCommand}"/>
                </TextBox.InputBindings>
            </TextBox>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancelar" 
                        IsCancel="True" 
                        Width="100" 
                        Height="32"
                        Margin="0,0,12,0"
                        Style="{StaticResource {x:Type Button}}"/>
                
                <Button Content="Aceptar" 
                        IsDefault="True" 
                        Click="Accept_Click"
                        Width="100" 
                        Height="32"
                        Style="{StaticResource PrimaryButtonStyle}"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
